// import React from 'react';
// import { mount } from 'enzyme';
// import Task from '../../src/scenes/dashboard/task/Task';
// import '../setupTest';
//
// import themeWrapper from '../mocks/ThemeWrapper';
//
// describe('Task component tests', () => {
//     it('Renders Task empty props', () => {
//         const wrapper = mount(
//             themeWrapper(
//                 <Task />,
//             ),
//         ).children();
//         //     .children();
//         // expect(wrapper.find('input.cd-name').length).toEqual(1);
//         // wrapper.find('input.cd-name').simulate('change', { target: { value: 'a' } });
//         // expect(wrapper.state().name).toEqual('a');
//         // expect(wrapper.find('textarea.cd-notes').length).toEqual(1);
//         // wrapper.find('textarea.cd-notes').simulate('change', { target: { value: 'my note' } });
//         // expect(wrapper.state().note).toEqual('my note');
//         // expect(wrapper.find('button.cd-blacklist').length).toEqual(1);
//         // // wrapper.find('button.cd-blacklist').simulate('click');
//         // expect(wrapper.find('button.cd-create-btn').length).toEqual(1);
//         // wrapper.setState({ options: [{ name: 'Comapany 1' }, { name: 'Comapany 2' }] });
//         expect(wrapper.length).toEqual(1);
//     });
// });
